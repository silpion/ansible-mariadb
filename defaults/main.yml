---

mariadb_port: 3306
mariadb_bind_address: "127.0.0.1"

mariadb_db: []
mariadb_users: []
mariadb_users_create: "{{ mariadb_users }}"

mariadb_additional_users: []

mariadb_repl_user: []

mariadb_repl_role: None
mariadb_db_id: 0

mariadb_config: []
mariadb_confd_file: "custom.cnf"

mariadb_enable_dump: false
mariadb_dump_username: 'mariadb_dump'
mariadb_dump_scriptfile: '/usr/local/sbin/mariadb_dump'
mariadb_dump_crontime:
  hour: 4
  dayofweek: '*'
